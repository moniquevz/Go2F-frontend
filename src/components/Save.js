import React from 'react'

function Save({ value,  color }) {
    return (
        <div className="save">
            <span>
                <i style={{ color }} className={
                    value = 'True'
                        ? 'fas fa-bookmark'
                        : value = "False"
                            ? 'far fa-bookmark alt'
                            : 'none'
                            
                }>

                </i>
            </span>

            

       

       

            

            
        </div>
    )
}

export default Save
